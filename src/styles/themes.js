// /src/styles/themes.js
import { colors } from "./colors";
import { fonts, fontSizes } from "./typography";

export const theme = {
  colors,
  fonts,
  fontSizes,
  spacing: {
    regular: 24,
    small: 16,
  },
  radius: 100,
};